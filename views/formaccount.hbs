<form class="form-horizontal" role="form" method="POST" accept-charset="utf-8" action="{{#if result.one}}/updateaccount{{else}}/addaccount{{/if}}">
  <div class="col-xs-12 col-sm-12">
    <h3>{{#if result.one}} {{else}}Nouveau compte{{/if}}</h3>
  </div>

  <div class="form-group">
    <div class="row">
    <label for="inputName" class="col-sm-2 control-label">Login {{#if result.one}} {{else}}/ Mot de passe{{/if}}</label>
    <div class="col-sm-3">
      <input type="text" class="form-control" placeholder="Login" id="login" name="login" required="required" value="{{#if result.one}}{{result.one.login}}{{/if}}">
    </div>
   

    {{#if result.one}}
    
    {{else}}
    <div class="col-sm-2">
      <input type="password" class="form-control" placeholder="Mot de passe" id="login" onchange="form.password2.pattern = this.value;" name="password" required="required" value="">
    </div>
    <div class="col-sm-2">
      <input type="password" class="form-control" placeholder="Confirmer le mot de passe" id="password2" name="password2" required="required" value="">
    </div>
    {{/if}}
    </div>
    </div>


    <div class="form-group">
      <div class="row">
    <label for="inputName" class="col-sm-2 control-label">Adresse email</label>
    <div class="col-sm-3">
      <div class="input-group"><span class="input-group-addon"><i class="fa fa-envelope"></i></span>
	<input class="form-control" type="email" placeholder="Adresse email" name="email" value="{{#if result.one}}{{result.one.email}}{{/if}}" title="'Ce champs est requis et doit être de la forme 'example@domain.com'" onchange="form.email2.pattern = this.value;" required="required"/>
      </div>
    </div>
      </div>
      </div>


    <div class="form-group">
      <div class="row">
    <label for="inputName" class="col-sm-2 control-label">Nom du rôle</label>
    <div class="col-sm-4">
      <select class="form-control" id="role" name="role" required="required">
          {{#compare url 'fiche_account' operator='indexof'}}
              {{#each result.Role}}
          <option value="{{this._id}}"  {{#compare this._id ../result.one.role operator='<'}} {{else}}{{#compare this._id ../../result.one.role operator='>'}} {{else}}selected{{/compare}}{{/compare}}>{{this.code}} - {{this.libelle}}</option>
              {{/each}}
          {{else}}
              {{#each result}}
                  <option value="{{this._id}}">{{this.code}} - {{this.libelle}}</option>
              {{/each}}
          {{/compare}}
      </select>
    </div>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      {{#if result.one}}
      <input type="hidden" name="_id" value="{{result.one._id}}"/>
      {{/if}}
      <button type="submit" name="button" id="button" class="btn btn-primary">Valider</button>
    </div>
  </div>

</form>


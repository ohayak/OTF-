{{#content "head"}}
    {{> head}}
{{/content}}
{{#content "nav"}}
  {{> nav}}
{{/content}}
{{#content "page"}}
      <!-- Content Wrapper. Contains page content -->
      <div class="content-wrapper">
        <!-- Content Header (Page header) -->
        <section class="content-header">
          <h1>
	    Comantaires
          </h1>
        </section>
        <!-- Main content -->
        <section class="content">
	  {{result.str}}
	  {{#each result}}
	  <div class="box box-primary bg-gray">
	    <div class="box-body">
	      <h3 class="box-title">{{id_auteur}}</h3>
	      <p>{{contenu_commentaire}}</p>
	      <p>Date {{date_commentaire}}</p>
	    </div>
	  </div>
	  {{/each}}
        </section><!-- /.content -->
      </div><!-- /.content-wrapper -->

{{/content}}
{{#content "footer"}}
    {{> footer}}
{{/content}}
